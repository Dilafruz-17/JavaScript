function oddNumber(n) {
    let arr = []

    for (let i = 0; i < n; i++) {
        arr.push(i)
    }

    let index = []

    for(let i = 0; i < arr.length; i++){
        
    }

    console.log(filtered, `toq sonlar soni ${filtered.length}`)
}
oddNumber(10)